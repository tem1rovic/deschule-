import React, { FC, memo } from 'react';


const Footer: FC = memo(() => {

    return (
            <div>
                <h1>Footer</h1>
            </div>
    );
});

Footer.displayName = 'Footer'

export default Footer;